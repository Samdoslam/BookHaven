let express,readdirSync,cors,mongoose;_6e8‍.w("express",[["default",["express"],function(v){express=v}]]);_6e8‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_6e8‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6e8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

const app = express();

// db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
  })
  .then(() => console.log("DB Connected"))
  .catch((err) => _6e8‍.g.console.log("DB Connection Error: ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json());

// route middleware
fs.readdirSync("./routes").forEach((file) => {
  _6e8‍.i(`./routes/${file}`).then((route) => {
    app.use("/api", route.default);
  });
});
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));
